/* Налаштування підключення до Supabase.
   Обидва значення ПУБЛІЧНІ (Settings → API → Project URL / anon public key).
   Service role key сюди НЕ вставляти ніколи. */
const CONFIG = {
  SUPABASE_URL: "https://XXXXXXXXXXXXXXXXXXXX.supabase.co",
  SUPABASE_ANON_KEY: "XXXX"
};
